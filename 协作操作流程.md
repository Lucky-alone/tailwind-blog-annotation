# 代码标注协作操作流程

> 本文档供组长和组员参考，按顺序执行即可完成全部协作流程。

---

## 阶段一：组长操作（初始化仓库）

### 1.1 在 GitHub 创建私有仓库

1. 打开 https://github.com/new
2. 填写仓库名称，例如 `tailwind-blog-annotation`
3. **选择 Private（私有）**，避免标注内容公开
4. **不要**勾选 "Add a README file"（本地已有代码）
5. 点击 **Create repository**

### 1.2 推送本地代码到 GitHub

在项目根目录打开终端，依次执行：

```bash
# 进入项目目录
cd d:\1\2\tailwind-blog

# 重命名 remote（保留原始 upstream 备用）
git remote rename origin upstream

# 添加你自己的 GitHub 仓库为 origin（替换为你的实际链接）
git remote add origin https://github.com/你的用户名/tailwind-blog-annotation.git

# 推送 main 分支
git push -u origin main

# 推送所有成员分支（member-1 到 member-5）
git push origin member-1
git push origin member-2
git push origin member-3
git push origin member-4
git push origin member-5
```

### 1.3 邀请组员为协作者

1. 进入你的 GitHub 仓库页面
2. 点击 **Settings** → **Collaborators** → **Add people**
3. 输入每位组员的 GitHub 用户名或邮箱，发送邀请
4. 组员接受邀请后即有推送权限

> **如果组员没有 GitHub 账号**：需要提前注册，注册地址 https://github.com/signup

### 1.4 将仓库链接发给组员

告诉组员：
```
仓库地址：https://github.com/你的用户名/tailwind-blog-annotation.git
你的分支：member-N（N 为你的编号 1~5）
负责的文件：见《代码标注分工方案.md》
```

---

## 阶段二：组员操作（代码标注）

### 2.1 克隆仓库（每人只执行一次）

```bash
# 克隆仓库（使用组长给的链接）
git clone https://github.com/组长用户名/tailwind-blog-annotation.git

# 进入项目目录
cd tailwind-blog-annotation
```

### 2.2 切换到自己的分支

```bash
# 切换到分配给自己的分支（以 member-1 为例）
git checkout member-1
```

### 2.3 查看自己负责的文件

查看 [代码标注分工方案.md](代码标注分工方案.md) 第三部分，确认自己负责哪些文件。

也可以用以下命令快速查看分工（以 member-1 为例）：

```bash
# 查看自己分支与 main 的差异文件列表（刚开始是空的，标注后会显示）
git diff --name-only main...member-1
```

### 2.4 进行代码标注

用任意编辑器（推荐 VS Code）打开自己负责的文件，在代码行末添加注释。

**标注格式**：
```typescript
const POSTS_PER_PAGE = 5   /* 每页显示文章数常量定义-张三标注 */
```

**标注要求**：
- 变量/常量：说明用途和含义
- 函数/方法：说明功能、参数、返回值
- 复杂逻辑：说明算法思路或业务逻辑
- 导入语句：说明引入的库/模块作用（可批量标注）
- JSX 结构：说明组件结构和渲染意图

> **重要**：只修改自己负责的文件，不要动其他人的文件！

### 2.5 提交并推送（可以分多次）

```bash
# 查看自己改了哪些文件
git status

# 添加所有修改
git add .

# 提交（写清楚说明）
git commit -m "张三：完成 app 页面路由层代码标注"

# 推送到远程分支
git push origin member-1
```

> 可以分批提交，比如每标注完 3~5 个文件就提交一次，防止丢失进度。

### 2.6 完成后通知组长

告诉组长："member-1 分支已推送完毕。"

---

## 阶段三：组长操作（检查与导出）

### 3.1 检查各成员进度

```bash
# 拉取所有分支的最新状态
git fetch --all

# 查看某成员修改了哪些文件（以 member-1 为例）
git diff --name-only main...member-1

# 查看某成员的提交记录
git log main...member-1 --oneline
```

### 3.2 导出标注文件

所有成员完成后，执行导出脚本：

```powershell
powershell -ExecutionPolicy Bypass -File export-annotations.ps1
```

脚本会自动生成：
```
export/
├── 组员1-代码标注/
│   └── app/（保留原始目录结构的标注文件）
├── 组员2-代码标注/
├── 组员3-代码标注/
├── 组员4-代码标注/
└── 组员5-代码标注/
```

> 导出前可编辑 `export-annotations.ps1` 中的 `Name` 字段，把"组员1"改为实际姓名。

### 3.3 打包提交

1. 将 `export/` 下的每个文件夹分别压缩为 `.rar`：
   - `张三代码标注.rar`
   - `李四代码标注.rar`
   - ...
2. 提交时注明：
   - **开源项目名称**：Tailwind Nextjs Starter Blog
   - **项目链接**：https://github.com/timlrx/tailwind-nextjs-starter-blog

---

## 常见问题

### Q: 组员 push 时提示没有权限？
确保组长已在 GitHub 仓库 Settings → Collaborators 中邀请了该组员。

### Q: 组员不小心改了别人的文件怎么办？
```bash
# 撤销某个文件的修改（恢复到分支初始状态）
git checkout main -- 被误改的文件路径
git add .
git commit -m "撤销误改文件"
```

### Q: 推送时提示冲突？
正常情况下不会冲突（每人改不同文件）。如果发生：
```bash
git pull --rebase origin member-1
# 解决冲突后
git add .
git push origin member-1
```

### Q: 组员更习惯用中文分支名？
不建议，Git 分支名使用英文和数字最稳定，中文可能在某些环境下乱码。
