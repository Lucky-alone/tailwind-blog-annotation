# 组员11 — 代码标注任务清单

## 负责模块：app 页面路由层（共 15 个文件，约 565 行）

请逐个打开以下文件，在代码行末添加标注注释。

## 文件列表

- [x] `app/layout.tsx` （111 行）— 根布局
- [x] `app/Main.tsx` （91 行）— 首页主体组件
- [x] `app/page.tsx` （9 行）— 首页入口
- [x] `app/not-found.tsx` （25 行）— 404 页面
- [x] `app/seo.tsx` （32 行）— SEO 工具函数
- [x] `app/robots.ts` （15 行）— robots.txt 生成
- [x] `app/sitemap.ts` （23 行）— sitemap 生成
- [x] `app/theme-providers.tsx` （12 行）— 主题 Provider
- [x] `app/about/page.tsx` （20 行）— 关于页
- [x] `app/api/newsletter/route.ts` （11 行）— 邮件订阅 API
- [x] `app/blog/page.tsx` （28 行）— 博客列表首页
- [x] `app/projects/page.tsx` （35 行）— 项目展示页
- [x] `app/tags/page.tsx` （41 行）— 标签列表页
- [x] `app/tags/[tag]/page.tsx` （59 行）— 标签文章列表页
- [x] `app/tags/[tag]/page/[page]/page.tsx` （53 行）— 标签分页

## 标注格式

```typescript
const POSTS_PER_PAGE = 5   /* 每页显示文章数常量定义-你的姓名标注 */
```

## 完成后

```bash
git add .
git commit -m "完成代码标注"
git push origin member-1
```
